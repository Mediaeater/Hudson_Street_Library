---json
{
  "permalink": "/assets/data/book-urls.json",
  "eleventyExcludeFromCollections": true
}
---
{
{%- for book in books %}
  "{{ book.id }}": "/books/{{ book.author_last | slugify }}_{{ book.title | slugify }}_{{ book.id }}/"{% if not loop.last %},{% endif %}
{%- endfor %}
}
